export type Website = {
  id?: string;
  name: string;
  url: string;
  icon: string;
  category: string;
  list: number;
};

export type SitePreview = {
  title: string;
  description: string;
  image: string;
};
